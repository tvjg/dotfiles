[include]
  path = ~/.gitconfig.local

[alias]
  ci = commit
  co = checkout
  st = status

[color]
  ui = auto

# Refuse to push if the upstream branch’s name is different from the local one
[push]
  default = simple
